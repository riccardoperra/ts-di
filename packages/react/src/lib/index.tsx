import React from 'react';

/* eslint-disable-next-line */
export interface ReactProps {}

export function Index(props: ReactProps) {
  return (
    <div>
      <h1>Welcome to react!</h1>
    </div>
  );
}

export default Index;
